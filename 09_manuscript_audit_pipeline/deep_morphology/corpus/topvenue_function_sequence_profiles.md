# Top-Venue Function Sequence Profiles

## Abstract

Observed dominant functions: `RESULT` 154, `INTERPRET` 143, `METHOD` 59, `PROBLEM` 59, `BG` 38, `SETUP` 38, `GAP` 16, `NEED` 14

Target sequence: `BG -> PROBLEM -> GAP -> TASK/METHOD -> SETUP -> RESULT -> INTERPRET/BOUNDARY`.

## Introduction

Observed dominant functions: `INTERPRET` 276, `BG` 99, `PROBLEM` 49, `RESULT` 26, `GAP` 23, `NEED` 15, `SETUP` 11, `METHOD` 5

## Results

Observed dominant functions: `RESULT` 175, `SETUP` 62, `ORG` 21, `PROBLEM` 11, `NEED` 2, `GAP` 1, `METHOD` 1

Target paragraph sequence: `QUESTION -> FIGURE/TABLE -> OBSERVATION -> QUANTIFICATION -> INTERPRETATION -> BOUNDARY -> TRANSITION`.

## Contribution

Observed dominant functions: `CONTRIB` 61

## Full-Section Templates

- Related Work: `CATEGORY -> REPRESENTATIVE WORKS -> COMMON CAPABILITY -> COMMON LIMITATION -> OUR DIFFERENCE`.
- Background: `DEFINE -> MECHANISM -> RELEVANCE -> LIMIT`.
- Method: `GOAL -> PRINCIPLE -> STRUCTURE -> FIXED VARIABLES -> CHANGED VARIABLES -> OUTPUT`.
- Experimental Setup: `PURPOSE -> PLATFORM -> TOOLCHAIN -> CONTROL -> VARIABLE -> METRIC -> REPEAT -> PROCESSING`.
- Discussion: `FINDING SYNTHESIS -> ALTERNATIVE EXPLANATION -> LIMITATION -> IMPLICATION`.
- Conclusion: `PROBLEM RECAP -> METHOD RECAP -> FINDING -> IMPLICATION -> BOUNDARY/FUTURE`.

## Corpus-to-Manuscript Coverage

- Top-venue sentence records: 1377.
- Manuscript sentence records: 292.
